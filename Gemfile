# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby ENV.fetch('RUBY_VERSION', '3.3.6')

gem 'pdf-reader'
gem 'sinatra'
gem 'json'
gem 'rackup'
gem 'puma'
